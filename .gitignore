node_modules/
data/*.db
data/*.db-shm
data/*.db-wal
.DS_Store

# Secrets — these must never be committed.
.env
bertuccilibrary-*.json
*-service-account*.json
*service_account*.json

# Cached cover images — rebuildable with: npm run covers
data/covers/

# Service logs
data/logs/

# Recipe-card images people upload in the meal planner, plus the titles and
# tags that go with them. Family data, not source: it belongs in a backup, not
# in git history.
data/meal-cards/
